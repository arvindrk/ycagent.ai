# `.agents/`

This folder is the repo-local home for Codex-readable rules and skills.

- `rules/`: always-on repository rules. Keep one topic per file.
- `skills/`: focused reusable workflows. Each skill has a `SKILL.md`.

Do not put drafts, scratch notes, generated logs, or secrets here. Codex can load these files as operating instructions.
